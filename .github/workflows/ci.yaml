name: CI/CD

on:
  push:
    branches:
      - main


jobs:
  release:
#    needs: test
    uses: FullStackS-GmbH/splunk-workflows/.github/workflows/build.yml@v1
    # Optional: pass a PAT/App token if you need workflow-triggering behavior
    # secrets:
    #   token: ${{ secrets.RELEASE_TOKEN }}
