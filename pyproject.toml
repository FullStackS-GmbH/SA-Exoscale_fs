[project]
name = "SA-Exoscale_fs"
version = "0.1.0"
description = "FULLSTACKS Add-on for Exoscale"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.5.1",
    "ruff>=0.15.1",
    "splunk-add-on-ucc-framework>=6.1.0",
]
